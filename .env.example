# AWS Bedrock credentials (required — export these before starting the backend)
# AWS_DEFAULT_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_key
# AWS_SECRET_ACCESS_KEY=your_secret
# AWS_SESSION_TOKEN=your_token

# bunq API key (leave empty to auto-create sandbox user)
BUNQ_API_KEY=
